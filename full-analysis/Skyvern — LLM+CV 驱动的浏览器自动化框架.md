🐉 Skyvern — LLM+CV 驱动的浏览器自动化框架元数据值仓库Skyvern-AI/skyvernStar~22,000+语言Python许可AGPL-3.0分类AI Agent / Browser Automation / RPA + LLM调研日期2026-06-14一、项目定位Skyvern 是一个利用大语言模型（LLM）和计算机视觉（CV）的浏览器自动化框架，填补了传统 RPA 工具（如 Selenium、Puppeteer）在「面对未知网站时的自适应能力」这一空白。它不再依赖预写的 XPath 或 CSS 选择器，而是让 AI 智能体「看懂」网页并自主决策下一步操作。一句话让 AI 像人一样「看」网页并操作，而非像爬虫一样解析 DOM。二、核心技术架构2.1 多智能体协作系统Skyvern 内部使用智能体群（swarm of agents） 架构，包含三个核心角色：智能体职责技术方案Planner（规划器）分析目标网页，制定步骤计划（共需几步、每步目标）LLM + 网页截图视觉分析Task（任务执行器）执行每一步具体操作（点击、输入、选择）Playwright + LLM 决策Validator（验证器）验证操作是否成功，检测页面状态变化视觉回归 + LLM 判断

### 优势

：即使网站改版，Planner + Validator 自动调整策略，无需改写代码。2.2 技术栈浏览器控制层：Playwright（Python SDK）视觉理解层：LLM（GPT-4V / Claude Vision / Gemini Vision），接收网页截图作为输入决策层：LLM 推理 + 结构化输出（JSON 格式的动作指令）凭证管理：内置 Bitwarden / 1Password / Skyvern Cloud 整合反检测机制：代理网络、CAPTCHA 自动求解、指纹隐匿2.3 AI-Augmented Playwright APISkyvern 的核心创新在于给 Playwright 的每个操作都增加了 AI 选项：# 传统方式page.click("#submit-button")# AI 增强方式page.click(prompt="点击绿色的提交按钮")这种设计让开发者可以逐步迁移：保留现有 Playwright 代码的同时，对易变部分启用 AI 回退。三、核心功能详解3.1 四大 AI 命令命令功能示例page.act()自然语言驱动的操作page.act("登录后等待仪表盘加载")page.extract()结构化数据提取page.extract("获取产品名称和价格")page.validate()页面状态验证page.validate("检查用户是否已登录")page.prompt()任意 LLM 提示page.prompt("分析这个页面的布局")3.2 高级 Agent API通过 page.agent 获取高阶能力：run_task(prompt) — 执行多步复杂任务login(credential_type, credential_id) — 自动登录（支持多种凭证管理器）download_files(prompt) — 导航并下载文件run_workflow(workflow_id) — 运行预构建工作流3.3 三种交互模式传统模式 — 直接使用 CSS / XPath 选择器AI 优先模式 — 自然语言描述目标元素AI 回退模式 — 先尝试选择器，失败时自动启用 AI四、性能与评测Skyvern 2.0 在 WebVoyager 基准测试中达到 85.8% 的成功率，已接近人类水平。指标传统 RPASkyvern 1.0Skyvern 2.0未知网站自适应❌ 不支持✅ 有限支持✅ 全面支持布局变更韧性❌ 脚本必须重写✅ 自动适应✅ 完全自适应单工作流多网站❌ 需逐一适配✅ 部分支持✅ 原生支持WebVoyager—~65%85.8%五、部署方式5.1 一键快速开始pip install "skyvern[all]"skyvern quickstart5.2 Docker 部署（含 UI + Postgres）git clone https://github.com/skyvern-ai/skyvern.gitdocker compose up -d# 访问 http://localhost:80805.3 云服务Skyvern Cloud 提供托管 API，包含反检测代理、CAPTCHA 求解和并行执行能力。5.4 多语言支持Python: pip install skyvernTypeScript: npm install @skyvern/client六、应用场景场景说明典型企业发票批量下载自动登录供应商门户批量下载 PDF财务自动化数据录入迁移从旧系统迁移数据至新平台IT 系统集成保险报价比价跨多个保险网站自动填表比价保险经纪电商竞品监控定时采集竞品价格和库存电商运营政府门户办事自动填写和提交各类政务表单企业合规社交媒体自动化跨平台内容发布和互动市场营销七、竞品对比特性SkyvernBrowserbase / StagehandPlaywright + AIAutoGPT（网页版）视觉理解✅ 原生 CV❌ 依赖 DOM❌ 需自行集成❌ 文本为主抗布局变化✅ 强⚠️ 中等❌ 弱❌ 弱反检测✅ 内置✅ 内置❌ 无❌ 无工作流 UI✅ 拖拽式❌ 代码定义❌ 代码定义❌ 代码定义凭证管理✅ Bitwarden/1Password❌ 无❌ 无❌ 无WebVoyager85.8%未公开未公开—八、总结与评价

### 优势

技术领先：LLM + CV 的组合使其在抗布局变化方面远超传统 RPA渐进式采用：AI 回退模式让现有 Playwright 用户无需重写全部代码生态完善：云服务 + 开源双重路径，Python + TypeScript 双 SDK性能可证：WebVoyager 85.8% 的公开评测数据有说服力不足依赖 LLM API：每次操作都调用 LLM，延迟和成本不可忽视复杂表单：对高度动态的 JS 渲染页面仍偶有失准企业部署：生产级部署需要 Postgres + 代理配置，面向开发者而非业务人员适用人群开发者：需要可编程、可靠的浏览器自动化运维/财务：需要自动化处理供应商门户等重复操作AI 应用开发者：需要将「网页操作能力」嵌入智能体系统中
